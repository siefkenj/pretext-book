import {
    AttrDisplay,
    ChildrenDisplay,
    ParentsDisplay,
    PtxExample,
    SyntaxDisplay,
} from "../../../components";

# `<ie>`

The self-closing `<ie/>` tag displays the abbreviation
 for the Latin <em>id est</em>.

## Syntax

<SyntaxDisplay name="ie" />

## Examples

 ```ptx-example
<FRAGMENT parents="p">
    Let <m>\varphi</m> be a homomorphism, <ie/>, a structure-preserving map.
</FRAGMENT>
```
