import {
    AttrDisplay,
    ChildrenDisplay,
    ParentsDisplay,
    PtxExample,
    SyntaxDisplay,
} from "../../../components";

# `<li>`

The `<li>` tag renders a list item. That is, if you have an ordered ([`<ol>`](ol)) or unordered ([`<ul>`](ul)) list,
every item in the list is surrounded by an `<li>` tag.

## Syntax

<SyntaxDisplay name="li" />
<ParentsDisplay name="li" variant="ElementLi1" />
<ParentsDisplay name="li" variant="ElementLi2" />

## Examples

```ptx-example
<FRAGMENT parents="p">
    <ul>
        <li>Item 1</li>
        <li>Item 2</li>
    </ul>
</FRAGMENT>
```
