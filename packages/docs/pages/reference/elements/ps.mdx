import {
    AttrDisplay,
    ChildrenDisplay,
    ParentsDisplay,
    PtxExample,
    SyntaxDisplay,
} from "../../../components";

# `<ps>`

The self-closing `<ps/>` outputs the an abbreviation of the latin _post scriptum_, usuall written as "p.s.".

## Syntax

<SyntaxDisplay name="ps" />

## Examples

```ptx-example
<FRAGMENT parents="p">
    This <ps/> that.
</FRAGMENT>
```
