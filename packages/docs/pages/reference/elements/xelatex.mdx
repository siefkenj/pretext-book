import {
    AttrDisplay,
    ChildrenDisplay,
    ParentsDisplay,
    PtxExample,
    SyntaxDisplay,
} from "../../../components";

# `<xelatex>`

The self-closing `<xelatex/>` tag inserts a formatted version of the XeLaTeX logo.

## Syntax

<SyntaxDisplay name="xelatex" />

## Examples

```ptx-example
<FRAGMENT parents="p">
    Typeset with <xelatex/>!
</FRAGMENT>
```