import {
    AttrDisplay,
    ChildrenDisplay,
    ParentsDisplay,
    PtxExample,
    SyntaxDisplay,
} from "../../../components";

# `<vs>`

The self-closing `<vs/>` outputs the an abbreviation of the latin _versus_, usuall written as "vs.".

## Syntax

<SyntaxDisplay name="vs" />

## Examples

```ptx-example
<FRAGMENT parents="p">
    This <vs/> that.
</FRAGMENT>
```