import {
    AttrDisplay,
    ChildrenDisplay,
    ParentsDisplay,
    PtxExample,
    SyntaxDisplay,
} from "../../../components";

# `<etc>`

The self-closing `<etc/>` tag displays the abbreviation (etc.)
 for the Latin <em>et caetera</em>.

## Syntax

<SyntaxDisplay name="etc" />

## Examples

 ```ptx-example
<FRAGMENT parents="p">
    Groups, rings, modules, <etc/>, are all algebraic objects.
</FRAGMENT>
```
