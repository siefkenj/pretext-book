import {
    AttrDisplay,
    ChildrenDisplay,
    ParentsDisplay,
    PtxExample,
    SyntaxDisplay,
} from "../../../components";

# `<q>`

The `<q>` tag wraps its content in double quotes.

## Syntax

<SyntaxDisplay name="q" />

## Examples

```ptx-example
<FRAGMENT parents="p">
    <q>No</q> means <em>no</em>!
</FRAGMENT>
```
