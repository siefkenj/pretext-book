import {
    AttrDisplay,
    ChildrenDisplay,
    ParentsDisplay,
    PtxExample,
    SyntaxDisplay,
} from "../../../components";

# `<frontmatter>`

Short description of what the `<frontmatter>` tag does.

## Syntax

<SyntaxDisplay name="frontmatter" />

## Examples

    