import {
    AttrDisplay,
    ChildrenDisplay,
    ParentsDisplay,
    PtxExample,
    SyntaxDisplay,
} from "../../../components";

# `<flat>`

The self-closing `<flat/>` tag displays the flat symbol in music notation. 

## Syntax

<SyntaxDisplay name="flat" />

## Examples

```ptx-example
<FRAGMENT parents="p">
    In the context of music, the flat symbol (<flat/>) can be used to specify
    key signatures or to modify a single note, e.g., A<flat/>. 
</FRAGMENT>
```
