import {
    AttrDisplay,
    ChildrenDisplay,
    ParentsDisplay,
    PtxExample,
    SyntaxDisplay,
} from "../../../components";

# `<swungdash>`

A decorative dash, like a tilde, but bigger, and centered.
The unicode character is &#x2053;, which is entered in HTML as `&#x2053;`.
Always used as a self-closing tag: `<swungdash/>`

## Syntax

<SyntaxDisplay name="swungdash" />

## Examples
```ptx-example
<FRAGMENT parents="p">
    Do you know what a swungdash looks like? <swungdash/> I don't.
</FRAGMENT>
```