import {
    AttrDisplay,
    ChildrenDisplay,
    ParentsDisplay,
    PtxExample,
    SyntaxDisplay,
} from "../../../components";

# `<langle>`

The left angle character: `\langle` in LaTeX and `&#x3008;` in HTML.
Used exclusively with the self-closing syntax `<langle/>`.

## Syntax

<SyntaxDisplay name="langle" />

## Examples
```ptx-example
<FRAGMENT parents="p">
    A <q>bra</q> state vector in quantum mechanics is written <langle/>a|.
</FRAGMENT>
```
    