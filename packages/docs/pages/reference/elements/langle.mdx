import {
    AttrDisplay,
    ChildrenDisplay,
    ParentsDisplay,
    PtxExample,
} from "../../../components";

# `<langle>`

The left angle character: `\langle` in LaTeX and `&#x3008;` in HTML.
Used exclusively with the self-closing syntax `<langle/>`.

## Syntax

### Attributes

<AttrDisplay name="langle" />

### Children

The following can be used as children.

<ChildrenDisplay name="langle" />

### Child of

This element can appear as an immediate child of the following elements.

<ParentsDisplay name="langle" />

## Examples
```ptx-example
<FRAGMENT parents="p">
    A <q>bra</q> state vector in quantum mechanics is written <langle/>a|.
</FRAGMENT>
```
    