import {
    AttrDisplay,
    ChildrenDisplay,
    ParentsDisplay,
    PtxExample,
    SyntaxDisplay,
} from "../../../components";

# `<section-mark>`

The stylized double-S to indicate section numbers.
The unicode character is &#xa7;, which is entered in HTML as `&#xa7;`.
Used with self-closing syntax: `<section-mark/>`

## Syntax

<SyntaxDisplay name="section-mark" />

## Examples
```ptx-example
<FRAGMENT parents="p">
    A section mark was used in <section-mark/>3.2.
</FRAGMENT>
```
    