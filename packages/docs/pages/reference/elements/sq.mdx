import {
    AttrDisplay,
    ChildrenDisplay,
    ParentsDisplay,
    PtxExample,
    SyntaxDisplay,
} from "../../../components";

# `<sq>`

The `<sq>` tag wraps its contents in single quote marks.

## Syntax

<SyntaxDisplay name="sq" />

## Examples

```ptx-example
<FRAGMENT parents="p">
    If double quotes are too scary, consider using
    <sq>single quotes</sq>.
</FRAGMENT>
```
