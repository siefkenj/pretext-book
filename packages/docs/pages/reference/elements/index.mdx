import {
    AttrDisplay,
    ChildrenDisplay,
    ParentsDisplay,
    PtxExample,
    SyntaxDisplay,
} from "../../../components";

# `<index>`

The `<index>` tag creates the back-matter division that holds a book or
article's generated index. It usually contains an optional
[`<title>`](./title.mdx) and an [`<index-list>`](./index-list.mdx),
while the actual entries are authored elsewhere with [`<idx>`](./idx.mdx).

## Syntax

<SyntaxDisplay name="index" />

## Examples

```ptx-example
<FRAGMENT parents="book backmatter">
    <index>
        <index-list />
    </index>
</FRAGMENT>
```

```ptx-example
<FRAGMENT parents="book backmatter">
    <index>
        <title>Index</title>
        <index-list />
    </index>
</FRAGMENT>
```