import {
    AttrDisplay,
    ChildrenDisplay,
    ParentsDisplay,
    PtxExample,
    SyntaxDisplay,
} from "../../../components";

# `<sharp>`

The self-closing `<sharp/>` tag displays the sharp symbol in music notation. 

## Syntax

<SyntaxDisplay name="sharp" />

## Examples

```ptx-example
<FRAGMENT parents="p">
    This <sharp/> will display the sharp symbol.
</FRAGMENT>
```

    