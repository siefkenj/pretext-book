import {
    AttrDisplay,
    ChildrenDisplay,
    ParentsDisplay,
    PtxExample,
    SyntaxDisplay,
} from "../../../components";

# `<sharp>`

The self-closing `<sharp/>` tag displays the sharp symbol in music notation. 

## Syntax

<SyntaxDisplay name="sharp" />

## Examples

```ptx-example
<FRAGMENT parents="p">
<p>
    In the context of music, the sharp symbol (<sharp/>) can be used to specify
    key signatures or to modify a single note, e.g., A<sharp/>. 
</p>
</FRAGMENT>
```
