import {
    AttrDisplay,
    ChildrenDisplay,
    ParentsDisplay,
    PtxExample,
} from "../../../components";

# `<rangle>`

The right angle character: `\rangle` in LaTeX and `&#x3009;` in HTML.
Used exclusively with the self-closing syntax `<rangle/>`.

## Syntax

### Attributes

<AttrDisplay name="rangle" />

### Children

The following elements can be used as children.

<ChildrenDisplay name="rangle" />

### Child of

This element can appear as an immediate child of the following elements.

<ParentsDisplay name="rangle" />

## Examples
```ptx-example
<FRAGMENT parents="p">
    A <q>ket</q> state vector in quantum mechanics is written |a<rangle/>.
</FRAGMENT>
```