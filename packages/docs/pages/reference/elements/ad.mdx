import {
    AttrDisplay,
    ChildrenDisplay,
    ParentsDisplay,
    PtxExample,
    SyntaxDisplay,
} from "../../../components";

# `<ad>`

The self-closing `<ad/>` tag displays the abbreviation
 for the Latin <em>anno Domini</em>.

## Syntax

<SyntaxDisplay name="ad" />

## Examples

 ```ptx-example
<FRAGMENT parents="p">
    Math XML became PreTeXt in 2017<ad/>.
</FRAGMENT>
```
