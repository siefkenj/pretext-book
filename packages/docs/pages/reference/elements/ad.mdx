import {
    AttrDisplay,
    ChildrenDisplay,
    ParentsDisplay,
    PtxExample,
    SyntaxDisplay,
} from "../../../components";

# `<ad>`

The self-closing `<ad/>` tag displays the abbreviation (AD)
 for the Latin <em>anno Domini</em> (in the year of our Lord) indicating a date
 after the birth of Christ, or secularly, in the Common Era (CE).

## Syntax

<SyntaxDisplay name="ad" />

## Examples

 ```ptx-example
<FRAGMENT parents="p">
    Math XML became PreTeXt in 2017 <ad/>.
</FRAGMENT>
```
