import {
    AttrDisplay,
    ChildrenDisplay,
    ParentsDisplay,
    PtxExample,
    SyntaxDisplay,
} from "../../../components";

# `<console>`

The `<console>` tag records a verbatim terminal session as a
sequence of [`<input>`](./input.mdx) and optional
[`<output>`](./output.mdx) elements. Prompts can be supplied on the
`<console>` itself or on individual inputs with the `prompt` and
`continuation` attributes.

If the session needs a number, title, or cross-reference target, wrap
the console in a [`<listing>`](./listing.mdx).

## Syntax

<SyntaxDisplay name="console" />

## Examples

```ptx-example
<FRAGMENT parents="section subsection">
    <console prompt="$ ">
        <input>python demo.py</input>
        <output>Hello, world!</output>
    </console>
</FRAGMENT>
```

```ptx-example
<FRAGMENT parents="section subsection">
    <console prompt="&gt;&gt;&gt; " continuation="... ">
        <input>for n in range(3):
    print(n)</input>
        <output>0
1
2</output>
    </console>
</FRAGMENT>
```