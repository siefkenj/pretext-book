import {
    AttrDisplay,
    ChildrenDisplay,
    ParentsDisplay,
    PtxExample,
} from "../../../components";

# `<obelus>`

A "division" symbol for use in text.
Always used with self-closing syntax `<obelus/>`.

## Syntax

### Attributes

<AttrDisplay name="obelus" />

### Children

The following elements can be used as children.

<ChildrenDisplay name="obelus" />

### Child of

This element can appear as an immediate child of the following elements.

<ParentsDisplay name="obelus" />

## Examples
```ptx-example
<FRAGMENT parents="p">
    If you still like to write division as 9<obelus/>3, you can do so with an obelus.
</FRAGMENT>
```
    