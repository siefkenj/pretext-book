import {
    AttrDisplay,
    ChildrenDisplay,
    ParentsDisplay,
    PtxExample,
} from "../../../components";

# `<code>`

Short description of what the `code` element does.

## Syntax

### Attributes

<AttrDisplay name="code" />

### Children

The following can be used as children.

<ChildrenDisplay name="code" />

### Child of

This element can appear as an immediate child of the following elements.

<ParentsDisplay name="code" />

## Examples

    