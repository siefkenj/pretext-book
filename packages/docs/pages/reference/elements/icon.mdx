import {
    AttrDisplay,
    ChildrenDisplay,
    ParentsDisplay,
    PtxExample,
    SyntaxDisplay,
} from "../../../components";

# `<icon>`

The self-closing `<icon>` tag produces icons known to Font Awesome and which have a unicode representation.

## Syntax

<SyntaxDisplay name="icon" />

## Examples

```ptx-example
<FRAGMENT parents="p">
    <c>&lt;icon name="gear"/&gt;</c> produces the gear icon
          <icon name="gear"/>.
</FRAGMENT>
```
