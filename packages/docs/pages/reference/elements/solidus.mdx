import {
    AttrDisplay,
    ChildrenDisplay,
    ParentsDisplay,
    PtxExample,
    SyntaxDisplay,
} from "../../../components";

# `<solidus>`

Fraction bar, not as steep as a forward slash.
Used to input a fraction without math mode.
The unicode character is &#x2044;, which is entered in HTML as `&#x2044;`.
Always used with self-closing syntax `<solidus/>`.

## Syntax

<SyntaxDisplay name="solidus" />

## Examples
```ptx-example
<FRAGMENT parents="p">
    Average velocity is computed as distance<solidus/>time.
</FRAGMENT>
```
    