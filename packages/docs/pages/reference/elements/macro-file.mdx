import {
    AttrDisplay,
    ChildrenDisplay,
    ParentsDisplay,
    PtxExample,
} from "../../../components";

# `<macro-file>`

Short description of what the `macro-file` element does.

## Syntax

### Attributes

<AttrDisplay name="macro-file" />

### Children

The following elements can be used as children.

<ChildrenDisplay name="macro-file" />

### Child of

This element can appear as an immediate child of the following elements.

<ParentsDisplay name="macro-file" />

## Examples

    