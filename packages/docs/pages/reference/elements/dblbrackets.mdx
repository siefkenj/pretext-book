import {
    AttrDisplay,
    ChildrenDisplay,
    ParentsDisplay,
    PtxExample,
    SyntaxDisplay,
} from "../../../components";

# `<dblbrackets>`

Wraps its content in double brackets.  Used for plain text (not math mode).

## Syntax

<SyntaxDisplay name="dblbrackets" />

## Examples

  ```ptx-example
<FRAGMENT parents="p">
    You might group <dblbrackets>stuff</dblbrackets> using double brackets.
</FRAGMENT>
``` 
    