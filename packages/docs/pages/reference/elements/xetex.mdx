import {
    AttrDisplay,
    ChildrenDisplay,
    ParentsDisplay,
    PtxExample,
    SyntaxDisplay,
} from "../../../components";

# `<xetex>`

The self-closing `<xetex/>` tag inserts a formatted XeTeX logo.

## Syntax

<SyntaxDisplay name="xetex" />

## Examples

```ptx-example
<FRAGMENT parents="p">
    Typeset with <xetex/>!
</FRAGMENT>
```

    