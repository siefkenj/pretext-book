import {
    AttrDisplay,
    ChildrenDisplay,
    ParentsDisplay,
    PtxExample,
    SyntaxDisplay,
} from "../../../components";

# `<ellipsis>`

Inserts the ellipsis. Used as the self-closing tag `<ellipsis/>`.
The unicode character is &#x2026;, which is entered in HTML as `&#x2026;`

## Syntax

<SyntaxDisplay name="ellipsis" />

## Examples
```ptx-example
<FRAGMENT parents="p">
    I wonder what an ellipsis does <ellipsis/>
</FRAGMENT>
```