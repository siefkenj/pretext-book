import {
    AttrDisplay,
    ChildrenDisplay,
    ParentsDisplay,
    PtxExample,
    SyntaxDisplay,
} from "../../../components";

# `<doublesharp>`

The self-closing `<doublesharp/>` tag displays the doublesharp symbol in music 
notation.

## Syntax

<SyntaxDisplay name="doublesharp" />

## Examples

```ptx-example
<FRAGMENT parents="p">
    In the context of music, the doublesharp symbol (<doublesharp/>) can be used
    to modify a single note, e.g., A<doublesharp/>.
</FRAGMENT>
```  
