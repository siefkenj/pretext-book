import {
    AttrDisplay,
    ChildrenDisplay,
    ParentsDisplay,
    PtxExample,
} from "../../../components";

# `<table>`

Short description of what the `table` element does.

## Syntax

### Attributes

<AttrDisplay name="table" />

### Children

The following elements can be used as children.

<ChildrenDisplay name="table" />

### Child of

This element can appear as an immediate child of the following elements.

<ParentsDisplay name="table" />

## Examples

    