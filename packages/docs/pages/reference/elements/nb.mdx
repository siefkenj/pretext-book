import {
    AttrDisplay,
    ChildrenDisplay,
    ParentsDisplay,
    PtxExample,
    SyntaxDisplay,
} from "../../../components";

# `<nb>`

The self-closing `<nb/>` tag displays the abbreviation
 for the Latin <em>nota bene</em>.

## Syntax

<SyntaxDisplay name="nb" />

## Examples

 ```ptx-example
<FRAGMENT parents="p">
    <nb/>:  Office hours next week will start at 7<am/>.
</FRAGMENT>
```
