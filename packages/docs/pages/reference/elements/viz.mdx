import {
    AttrDisplay,
    ChildrenDisplay,
    ParentsDisplay,
    PtxExample,
    SyntaxDisplay,
} from "../../../components";

# `<viz>`

The self-closing `<viz/>` outputs the an abbreviation of the latin _videlicet_, usuall written as "viz.".

## Syntax

<SyntaxDisplay name="viz" />

## Examples

```ptx-example
<FRAGMENT parents="p">
    Using latin phrases like <q><viz/></q> make you sound fancy.
</FRAGMENT>
```
