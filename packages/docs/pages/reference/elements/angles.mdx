import {
    AttrDisplay,
    ChildrenDisplay,
    ParentsDisplay,
    PtxExample,
    SyntaxDisplay,
} from "../../../components";

# `<angles>`

Wraps its content in angle brackets.  For plain text (not math mode).

## Syntax

<SyntaxDisplay name="angles" />

## Examples

  ```ptx-example
<FRAGMENT parents="p">
    Sometimes people write <angles>something like this</angles> to
    represent a blank that is grouped together. 
</FRAGMENT>
``` 