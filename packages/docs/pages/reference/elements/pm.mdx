import {
    AttrDisplay,
    ChildrenDisplay,
    ParentsDisplay,
    PtxExample,
    SyntaxDisplay,
} from "../../../components";

# `<pm>`

The self-closing `<pm/>` tag displays the abbreviation
 for the Latin <em>post meridiem</em>.

## Syntax

<SyntaxDisplay name="pm" />

## Examples

 ```ptx-example
<FRAGMENT parents="p">
    Office hours will be from 11<am/> until 1<pm/>.
</FRAGMENT>
```
