import {
    AttrDisplay,
    ChildrenDisplay,
    ParentsDisplay,
    PtxExample,
    SyntaxDisplay,
} from "../../../components";

# `<pm>`

The self-closing `<pm/>` tag displays the abbreviation (PM)
 for the Latin <em>post meridiem</em> (after noon) indicating times between noon and midnight.

## Syntax

<SyntaxDisplay name="pm" />

## Examples

 ```ptx-example
<FRAGMENT parents="p">
    Office hours will be from 11<am/> until 1<pm/>.
</FRAGMENT>
```
