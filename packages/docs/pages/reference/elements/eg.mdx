import {
    AttrDisplay,
    ChildrenDisplay,
    ParentsDisplay,
    PtxExample,
    SyntaxDisplay,
} from "../../../components";

# `<eg>`

The self-closing `<eg/>` tag displays the abbreviation (e.g.)
 for the Latin <em>exempli gratia</em> (for example).

## Syntax

<SyntaxDisplay name="eg" />

## Examples

 ```ptx-example
<FRAGMENT parents="p">
    Mathematics has many disciplines, <eg/>, algebra, analysis, and topology.
</FRAGMENT>
```
    