import {
    AttrDisplay,
    ChildrenDisplay,
    ParentsDisplay,
    PtxExample,
    SyntaxDisplay,
} from "../../../components";

# `<doubleflat>`

The self-closing `<doubleflat/>` tag displays the doubleflat symbol in music notation. 

## Syntax

<SyntaxDisplay name="doubleflat" />

## Examples

```ptx-example
<FRAGMENT parents="p">
<p>
    In the context of music, the doubleflat symbol (<doubleflat/>) can be used
    to modify a single note, e.g., A<doubleflat/>.
</p>
</FRAGMENT>
```    
