import {
    AttrDisplay,
    ChildrenDisplay,
    ParentsDisplay,
    PtxExample,
    SyntaxDisplay,
} from "../../../components";

# `<am>`

The self-closing `<am/>` tag displays the abbreviation (AM)
 for the Latin <em>ante meridiem</em> (before noon) indicating times between
 midnight and noon.

## Syntax

<SyntaxDisplay name="am" />

## Examples

```ptx-example
<FRAGMENT parents="p">
    7<am/> is a challenging hour for most students.
</FRAGMENT>
```
