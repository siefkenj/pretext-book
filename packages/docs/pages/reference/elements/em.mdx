import {
    AttrDisplay,
    ChildrenDisplay,
    ParentsDisplay,
    PtxExample,
    SyntaxDisplay,
} from "../../../components";

# `<em>`

Wrapper for text that should be *emphasized* (often rendered italic).

See also [`<alert>`](./alert).

## Syntax

<SyntaxDisplay name="em" />

## Examples

  ```ptx-example
<FRAGMENT parents="p">
    PreTeXt isn't <em>that</em> hard to learn.
</FRAGMENT>
``` 
    