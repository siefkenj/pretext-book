import {
    AttrDisplay,
    ChildrenDisplay,
    ParentsDisplay,
    PtxExample,
    SyntaxDisplay,
} from "../../../components";

# `<times>`

The self-closing `<times/>` tag provides "multiplication sign" symbol for use in text.
The unicode character is &#xd7;, which is entered in HTML as `&#xd7;`.
Used with self-closing syntax: <times/>.

## Syntax

<SyntaxDisplay name="times" />

## Examples
```ptx-example
<FRAGMENT parents="p">
    It would be strange to use math mode to input <q>humans<times/>cats</q>.
</FRAGMENT>
```
    