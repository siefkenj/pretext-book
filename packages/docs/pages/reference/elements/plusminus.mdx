import {
    AttrDisplay,
    ChildrenDisplay,
    ParentsDisplay,
    PtxExample,
    SyntaxDisplay,
} from "../../../components";

# `<plusminus>`

The combined symbol. 
The unicode symbol is &#xb1;, which is entered in HTML as `&#xb1;`.
Always used with self-closing syntax `<plusminus/>`.

## Syntax

<SyntaxDisplay name="plusminus" />

## Examples
```ptx-example
<FRAGMENT parents="p">
  I am correct 50<plusminus/>40% of the time.
</FRAGMENT>
```