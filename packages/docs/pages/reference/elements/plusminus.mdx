import {
    AttrDisplay,
    ChildrenDisplay,
    ParentsDisplay,
    PtxExample,
} from "../../../components";

# `<plusminus>`

The combined symbol. Always used with self-closing syntax `<plusminus/>`.

## Syntax

### Attributes

<AttrDisplay name="plusminus" />

### Children

The following elements can be used as children.

<ChildrenDisplay name="plusminus" />

### Child of

This element can appear as an immediate child of the following elements.

<ParentsDisplay name="plusminus" />

## Examples
```ptx-example
<FRAGMENT parents="p">
  I am correct 50<plusminus/>40% of the time.
</FRAGMENT>
```