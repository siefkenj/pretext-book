import {
    AttrDisplay,
    ChildrenDisplay,
    ParentsDisplay,
    PtxExample,
    SyntaxDisplay,
} from "../../../components";

# `<attr>`

Used when naming a PreTeXt (or other XML) attribute, as in,
`The <attr>width</attr> controls the horizontal size of an image`

See also [`<tag>`](./tag) and [`<tage>`](./tage) for other markup-describing
elements.

## Syntax

<SyntaxDisplay name="attr" />

## Examples

  ```ptx-example
<FRAGMENT parents="p">
    You can specify the width of an image with the <attr>width</attr> 
    attribute. 
</FRAGMENT>
```   