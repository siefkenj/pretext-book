# `component`

The `component` attribute is used to assign _version_ to a prarticular part of your document. When compiling,
you (the publisher) can choose which _component_s to include in the final output.

## Examples

    