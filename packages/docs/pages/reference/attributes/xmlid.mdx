# `xml:id`

The `xml:id` attribute is used to assign an ID to an element. This ID can be used to reference the element in other parts of the document
with the [`<xref>`](../elements/xref) tag.

## Examples

    