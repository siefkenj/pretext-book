# `xml:lang`

The `xml:lang` attribute is used to specify the language of all of or a portion of the document. This affects how
certain parts of the document will be localized (e.g., whether a `<chapter>` is displayed as "Chapter" or "Capítulo").

## Examples

    